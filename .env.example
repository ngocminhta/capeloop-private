# Naming reference only: the CLI does not auto-load this file.
# Export the needed value in your shell and never commit a real credential.
OPENAI_API_KEY=

# Example dedicated credential name for an explicitly reviewed custom endpoint.
# Do not put an OpenAI credential in this variable.
CAPE_LOOP_PROXY_KEY=
